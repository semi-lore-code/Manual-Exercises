// Printable.java
// The interface defines a contract for any class that can be printed
public interface Printable {
    // Interface methods are public and abstract by default
    void print();
}
